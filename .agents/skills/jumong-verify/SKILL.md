---
name: jumong-verify
description: 전체 2D 주몽신화전기 Godot 프로젝트의 구현 결과를 읽기 전용으로 실행·검사하고 화면, 로그, 종료 코드, 씬 상태와 2D 제작 원칙에 근거해 판정한다. Godot 스크립트·씬·애셋 변경 후 스모크 테스트, 파싱·런타임 오류, 3D 의존성 유입, 기능 동작과 마일스톤 증거를 확인할 때 사용한다.
---

# 주몽 검증

## 읽기 전용 경계

- 코드, 씬, 리소스, 애셋을 생성·수정·삭제하지 않는다.
- 검증 도구가 만드는 Godot 캐시와 로그 외에는 프로젝트 상태를 변경하지 않는다.
- 실패를 고치지 말고 재현 조건과 수정 필요 사항만 보고한다.
- 검증 때문에 임포트 파일이나 프로젝트 파일이 바뀌면 그 사실을 판정과 분리해 보고한다.

## 검증 절차

1. `AGENTS.md`, `$jumong-production-governor`의 전체 2D 불변 조건, 승인 기준과 변경 파일 목록을 확인한다.
2. 사용 가능한 Godot 실행 파일과 프로젝트 메인 씬을 읽기 전용으로 확인한다.
3. 위험이 낮은 검사부터 수행한다.
   - 승인된 신규·변경 씬에 Node3D, Camera3D, 3D 조명·물리·메시 의존성이 유입되지 않았는지 확인
   - 프로젝트 헤드리스 에디터 로드
   - 지정 씬 또는 프로젝트 헤드리스 실행
   - 필요한 경우 화면 실행과 스크린샷 캡처
4. 명령, Godot 버전, 실행 대상, 종료 코드를 기록한다.
5. 파싱 오류, 런타임 오류, 경고와 디버그 출력을 확인한다.
6. 승인 기준마다 관찰 가능한 증거를 연결한다.
7. 아래 판정 규칙과 보고 형식으로 결과를 제출한다.

GUI 실행이나 외부 도구 권한이 필요하면 승인을 요청한다. 화면을 확인할 수 없다면 로그 성공을 화면 성공으로 대신하지 말고 `검증 불가` 또는 `부분 통과`로 판정한다.

## 판정 규칙

- `통과`: 모든 승인 기준이 적절한 증거로 확인되고 차단 오류가 없다.
- `부분 통과`: 일부 기준만 확인되었거나 비차단 경고·미확인 항목이 남는다.
- `실패`: 오류가 재현되거나 승인 기준과 다른 동작이 관찰된다.
- `검증 불가`: 실행 환경, 권한, 의존성 또는 증거 부족으로 판정할 수 없다.

종료 코드 0만으로 기능을 통과시키지 않는다. 오류가 없다는 사실과 요청 기능이 동작한다는 사실을 별도로 검증한다.

## 보고 형식

```text
검증 대상:
승인 기준:
실행 환경:
실행 명령 및 종료 코드:

판정: 통과 | 부분 통과 | 실패 | 검증 불가

기준별 확인 결과:
- 기준:
  관찰:
  증거:

오류 및 경고:
- 개수와 주요 메시지

증거:
- 스크린샷 경로
- 로그 또는 관련 출력
- 씬 트리·상태 정보

미확인 항목:
다음 조치 및 재검증 항목:
```

증거가 없으면 `확보하지 못함`과 이유를 적는다. 검증되지 않은 항목을 완료라고 표현하지 않는다.
